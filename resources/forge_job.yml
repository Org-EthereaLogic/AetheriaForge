resources:
  jobs:
    forge_job:
      name: "aetheriaforge_forge"
      tasks:
        - task_key: run_forge_pipeline
          notebook_task:
            notebook_path: ../notebooks/02_run_forge_pipeline.py
            base_parameters:
              catalog: "${var.catalog}"
              schema: "${var.schema}"
              contract_path: "${var.contract_path}"
              evidence_dir: "${var.evidence_dir}"
