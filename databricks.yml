# AetheriaForge Databricks Asset Bundle
# See: https://docs.databricks.com/dev-tools/bundles/

bundle:
  name: aetheriaforge

include:
  - "resources/*.yml"

variables:
  catalog:
    description: Existing Unity Catalog catalog name
  schema:
    description: Unity Catalog schema name
    default: default
  contract_path:
    description: Path to the forge contract YAML (leave blank for template/demo)
    default: ""
  evidence_dir:
    description: Evidence output directory
    default: /tmp/aetheriaforge_evidence

targets:
  dev:
    default: true
    mode: development
  prod:
    mode: production
