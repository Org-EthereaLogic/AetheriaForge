command:
  - gradio
  - app.py

# When deployed via the bundle, env vars CONTRACTS_DIR and EVIDENCE_DIR are
# injected by the app resource definition in resources/aetheriaforge_app.yml.
# When running locally, the app falls back to ~/.aetheriaforge/ defaults.
