# Databricks Apps installs from the snapshot of the bundle-uploaded app/ dir.
# The package is pulled from PyPI so the app source stays under the 10 MB limit.
etherealogic-aetheriaforge
gradio>=6.10.0,<7
plotly>=5.0,<6
