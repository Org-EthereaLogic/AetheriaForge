# docs

Explanatory documentation for ÆtheriaForge. `docs/` must not override
`specs/` when they diverge. `specs/` is the canonical SDLC surface.

Current contents:

- `customer_impact_advisory_v0_1_4.md` — customer-facing advisory for users of
  pre-`0.1.4` releases, including exact rerun guidance.
- `notion_dashboard_sync.md` — policy and workflow for syncing repo state to
  the public Notion dashboard (external coordination surface only).
